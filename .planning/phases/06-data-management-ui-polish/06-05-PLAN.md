---
phase: 06-data-management-ui-polish
plan: 05
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - app/(auth)/login.tsx
  - app/(auth)/signup.tsx
  - app/(auth)/forgot-password.tsx
  - app/(auth)/verify-reset-code.tsx
  - app/(onboarding)/welcome.tsx
  - app/(onboarding)/profile.tsx
  - app/(onboarding)/how-it-works.tsx
  - app/(onboarding)/celebration.tsx
  - app/(onboarding)/index.tsx
  - src/components/daily/QODStep.tsx
  - src/components/daily/BehaviorStep.tsx
  - src/components/daily/CompletionCelebration.tsx
autonomous: true

must_haves:
  truths:
    - "All 4 auth screens use colors from useTheme() hook, not hardcoded hex values"
    - "All 5 onboarding screens use colors from useTheme() hook, not hardcoded hex values"
    - "All 3 daily assessment components use colors from useTheme() hook, not hardcoded hex values"
    - "All interactive elements have 44pt minimum touch targets"
    - "Auth screens maintain their existing visual design, just sourcing colors from theme"
    - "Onboarding screens keep their brand-heavy celebratory feel with theme tokens"
  artifacts:
    - path: "app/(auth)/login.tsx"
      provides: "Login screen with themed colors"
      contains: "useTheme"
    - path: "app/(onboarding)/welcome.tsx"
      provides: "Welcome onboarding screen with themed colors"
      contains: "useTheme"
    - path: "src/components/daily/QODStep.tsx"
      provides: "QOD step component with themed colors"
      contains: "useTheme"
  key_links:
    - from: "app/(auth)/login.tsx"
      to: "src/theme/ThemeContext.tsx"
      via: "useTheme for all color values"
      pattern: "useTheme"
    - from: "app/(onboarding)/welcome.tsx"
      to: "src/theme/ThemeContext.tsx"
      via: "useTheme for brand colors"
      pattern: "useTheme"
    - from: "src/components/daily/QODStep.tsx"
      to: "src/theme/ThemeContext.tsx"
      via: "useTheme for assessment colors"
      pattern: "useTheme"
---

<objective>
Migrate all auth screens (4), onboarding screens (5), and daily assessment components (3) from hardcoded colors to useTheme().

Purpose: These 12 files contain approximately 82 hardcoded color references. Auth screens are the first thing users see; onboarding is the first experience for new students; daily components are used every day. All must use theme tokens for dark mode readiness and visual consistency.

Output: All auth, onboarding, and daily component files use useTheme() for colors. Zero hardcoded hex values remain in these files (excluding shadow/rgba).
</objective>

<execution_context>
@/Users/robertisrael/.claude/get-shit-done/workflows/execute-plan.md
@/Users/robertisrael/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-data-management-ui-polish/06-CONTEXT.md
@.planning/phases/06-data-management-ui-polish/06-RESEARCH.md
@.planning/phases/06-data-management-ui-polish/06-01-SUMMARY.md
@src/theme/colors.ts
@src/theme/index.ts
@src/theme/ThemeContext.tsx
@app/(auth)/login.tsx
@app/(auth)/signup.tsx
@app/(onboarding)/welcome.tsx
@src/components/daily/QODStep.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate auth screens and onboarding screens to useTheme</name>
  <files>
    app/(auth)/login.tsx
    app/(auth)/signup.tsx
    app/(auth)/forgot-password.tsx
    app/(auth)/verify-reset-code.tsx
    app/(onboarding)/welcome.tsx
    app/(onboarding)/profile.tsx
    app/(onboarding)/how-it-works.tsx
    app/(onboarding)/celebration.tsx
    app/(onboarding)/index.tsx
  </files>
  <action>
    Migrate all auth and onboarding screen files to use `useTheme()` instead of hardcoded hex colors.

    **For each file:**
    1. Add `import { useTheme } from '@/theme';`
    2. Add `const { colors } = useTheme();` at top of component
    3. Replace hardcoded hex colors using the standard mapping:
       - `#4F46E5` -> `colors.primary`
       - `#EEF2FF` -> `colors.primaryLight`
       - `#F3F4F6` -> `colors.backgroundSecondary`
       - `#fff`/`#ffffff` -> `colors.background` or `colors.card` or `colors.textInverse`
       - `#111827` -> `colors.text`
       - `#6B7280` -> `colors.textSecondary`
       - `#9CA3AF` -> `colors.textTertiary`
       - `#E5E7EB` -> `colors.border`
       - `#10B981` -> `colors.success`
       - `#EF4444`/`#DC2626` -> `colors.error`
       - `#3B82F6` -> `colors.info`
       - `#F97316` -> `colors.warning`
       - Any other hex -> find closest theme token
    4. For screens with heavy inline styles, convert to `createStyles(colors)` factory pattern
    5. Ensure 44pt minimum touch targets on all interactive elements

    **Auth screens (login, signup, forgot-password, verify-reset-code):**
    These are outside the tab navigator but inside ThemeProvider (wired in Plan 04). They have ~45 hardcoded values total. Important: these screens have their own layout backgrounds (often white or light gray) and primary-colored buttons. Keep the existing visual design, just source all colors from theme tokens.

    **Onboarding screens (welcome, how-it-works, profile, celebration, index):**
    These are seen once during first student login and have brand-heavy styling. Convert hardcoded indigo/purple to theme primary. Keep the celebratory feel for the celebration screen. These have ~37 hardcoded color values total.
  </action>
  <verify>
    - `npx tsc --noEmit` passes
    - Grep for hardcoded hex colors in `app/(auth)/` and `app/(onboarding)/` shows zero matches (excluding shadowColor '#000' and rgba)
    - All 9 files import and use useTheme()
    - All touch targets >= 44pt
  </verify>
  <done>
    All 4 auth screens and 5 onboarding screens use useTheme() for colors. Zero hardcoded hex values remain in auth and onboarding files.
  </done>
</task>

<task type="auto">
  <name>Task 2: Migrate daily assessment components to useTheme</name>
  <files>
    src/components/daily/QODStep.tsx
    src/components/daily/BehaviorStep.tsx
    src/components/daily/CompletionCelebration.tsx
  </files>
  <action>
    Migrate all 3 daily assessment components to use `useTheme()` instead of hardcoded hex colors.

    **For each component:**
    1. Add `import { useTheme } from '@/theme';`
    2. Add `const { colors } = useTheme();` at top of component
    3. Replace all hardcoded hex colors using the standard mapping (same as Task 1)
    4. For components with many hardcoded values, convert to `createStyles(colors)` factory pattern
    5. Ensure 44pt minimum touch targets on answer buttons and Slider components

    **Component-specific notes:**

    - **QODStep.tsx:** Has answer option cards with correct/incorrect coloring (green/red). Use `colors.success` and `colors.error` for these semantic states. Keep the intermediate celebration animation styling but source colors from theme.

    - **BehaviorStep.tsx:** Has Slider components with category labels and score displays. The slider track colors may need special handling -- use `colors.primary` for the active track and `colors.border` for inactive track.

    - **CompletionCelebration.tsx:** Has celebratory styling with stars/confetti feel. Keep the visual energy but source colors from theme tokens. Use `colors.primary` for the main celebration color, `colors.success` for positive reinforcement elements.

    **The `@/` import alias already works for these components** since they're in `src/components/`.
  </action>
  <verify>
    - `npx tsc --noEmit` passes
    - Grep for hardcoded hex colors in `src/components/daily/` shows zero matches (excluding shadowColor '#000' and rgba)
    - All 3 files import and use useTheme()
    - All touch targets >= 44pt on answer buttons and interactive elements
  </verify>
  <done>
    All 3 daily assessment components (QODStep, BehaviorStep, CompletionCelebration) use useTheme() for colors. Zero hardcoded hex values remain in daily component files.
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with no type errors
2. All 12 files (4 auth + 5 onboarding + 3 daily) use useTheme() hook for colors
3. Grep for `#[0-9a-fA-F]{6}` in auth, onboarding, and daily directories returns zero (excluding shadow/rgba)
4. No `import { colors } from` static imports remain in these files
5. All touch targets >= 44pt
6. Auth screens maintain their existing visual design
7. Onboarding screens keep their celebratory brand feel
</verification>

<success_criteria>
- All 4 auth screens fully themed with useTheme()
- All 5 onboarding screens fully themed with useTheme()
- All 3 daily assessment components fully themed with useTheme()
- Zero hardcoded hex colors outside theme definitions and shadow/rgba exceptions
- 44pt minimum touch targets on all interactive elements
- Existing visual design preserved -- only color source changed, not the actual colors
</success_criteria>

<output>
After completion, create `.planning/phases/06-data-management-ui-polish/06-05-SUMMARY.md`
</output>
